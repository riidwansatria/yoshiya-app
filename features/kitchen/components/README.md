# Kitchen Feature Components

Place new kitchen-scoped composition components here.

Guidelines:

- Keep this layer presentational.
- Server calls should go through `features/kitchen/server`.
- Reuse shared primitives from `components/ui`.
